{
  "build": {
    "env": {
      "PNPM_HOME": "/vercel/.pnpm",
      "PATH": "/vercel/.pnpm:$PATH"
    },
    "commands": [
      "corepack enable",
      "corepack prepare pnpm@latest --activate",
      "pnpm install",
      "pnpm build"
    ]
  }
}